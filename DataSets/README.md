## Data

The datasets used in this case study are synthetically generated inside the notebook.

Delta table files are not included in this repository because they are generated locally and can become large.

To reproduce the experiment, run the data generation section of the notebook.
